from __future__ import annotations


# Hidden code switch. Keep disabled for the current preview-image-only build.
# Set this to True when archive/PSD/other attachment downloads should be enabled again.
INCLUDE_OTHER_ATTACHMENTS = False

# Hidden default download concurrency. Keep the GUI simple; change this number when needed.
DEFAULT_CONCURRENCY = 3
